# Java 13 - Heranca - Questoes

---

## Atividade 1 - Executando o codigo

**O que fazer:**

- Execute o `Principal.java` e observe a saida.

**Cole a saida completa do terminal abaixo.**

```text

```

---

## Atividade 2 - Lendo o codigo

**Abra `Aluno.java` e responda:**

**O que significa `extends Pessoa` na declaracao da classe?**

R.

**O que faz a linha `super(nome, idade)` no construtor de `Aluno`?**

R.

**Por que `nome` e `idade` sao `protected` em `Pessoa` e nao `private`?**

R.

---

## Atividade 3 - Atributos e metodos herdados

**Sem abrir o codigo, liste o que `Aluno` possui:**

**Atributos herdados de `Pessoa`:**

R.

**Atributos proprios de `Aluno`:**

R.

**Metodos herdados de `Pessoa`:**

R.

**Metodos proprios de `Aluno`:**

R.

---

## Atividade 4 - instanceof

**Analise a saida do `instanceof` e responda:**

**Por que `aluno1 instanceof Pessoa` retorna `true`?**

R.

**Por que `pessoa1 instanceof Aluno` retorna `false`?**

R.

**O que a relacao "is-a" significa em orientacao a objetos?**

R.

---

## Atividade 5 - Experimentando

**O que fazer:**

- Crie uma terceira classe `Professor.java` no mesmo pacote que herde de `Pessoa`.
- Adicione o atributo `private String disciplina`.
- Crie o construtor usando `super()`.
- No `Principal.java`, instancie um `Professor` e chame `apresentar()`.

**Cole o codigo de `Professor.java` abaixo.**

```java

```

**Cole a saida adicional do terminal.**

```text

```

---

## Atividade 6 - Perguntas finais

**O que e heranca em Java?**

R.

**Qual palavra-chave usamos para herdar de uma classe?**

R.

**O que faz `super()` no construtor?**

R.

**Qual e a diferenca entre atributo `private` e `protected` em relacao a heranca?**

R.

**Java permite heranca multipla (uma classe herdar de duas ao mesmo tempo)?**

R.

---
